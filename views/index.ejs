<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <ul>
      <% for(var i=0; i < messages.length; i++) { %>
         <li>
            <p id="msg"><%= messages[i].text %></p>
            <p><%= messages[i].user %></p>
            <p><%= messages[i].added %></p>
         </li>
      <% } %>
    </ul>

    <a href="/new">Enter a new message for the board ...</a>

  </body>
</html>
